#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
	/*int t =2;
    char *str[t];
    for(int i=0;i<t;i++){
        str[i] = (char *)malloc(1000);
        fgets(str[i],1000,stdin);
    }
    int min = strlen(str[0]);
    for (int i=1;i<t;i++){
    	if (strlen(str[i])<min){
    		min = strlen(str[i]);
    	}
    }
 

    for (int i=0;i<t;i++) free(str[i]);*/
    printf("%d\n", (332/10)%10);
    return 0;
}
